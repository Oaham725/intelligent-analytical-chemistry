# 《智能分析化学》课程共建指南

本仓库同时服务在线教材与课程大作业。所有合并到 `main` 的修改都将成为后续学生可阅读、可复用和可继续修订的教材内容。

## 开始前

1. 阅读网站中的“课程共建”页面和所认领章节。
2. 在 Issue 中说明要解决的问题、影响文件和验收方式。
3. 从最新 `main` 创建独立分支；不要直接修改 `main`。

## 文件约定

| 内容 | 位置 |
|---|---|
| 章节正文 | `docs/chXX.md` |
| 本章代码 | `code/chXX.py` 或 `code/chXX_exercises/` |
| 本章知识图谱 | `assets/graphs/chXX.svg` |
| Word 下载稿 | `downloads/word/chXX.docx` |
| 课程运行规则 | `docs/course/` |

不要重命名其他小组已使用的文件；若必须移动文件，请同时更新章节中的链接。

## 提交前自查

- 正文标题层级、术语和公式符号与章节上下文一致；
- Python 文件可从仓库根目录运行，或在 PR 中写明必要依赖；
- 图表优先提供 SVG；图中信息能在正文中得到解释；
- 没有加入学号、邮箱、手机号、访问令牌或无权公开的数据；
- PR 关联一个 Issue，并接受至少一次交叉评审。

详细流程与评分规则见网站的“课程共建”栏目。
